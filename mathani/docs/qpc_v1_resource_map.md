# خريطة موارد مصحف المدينة - الرسم القديم (QPC V1) 🗺️

هذا المستند مخصص لمساعدة المطورين في فهم هيكلية المصحف القديم في تطبيق "مثاني".

## 1. روابط الموارد (Assets URLs) 🔗

| النوع | الرابط | الوصف |
| :--- | :--- | :--- |
| **قاعدة بيانات التوزيع** | `assets/data/qpc_v1_layout.db` | تحتوي على إحداثيات الكلمات وتوزيع الأسطر لكل صفحة. |
| **حزمة الخطوط** | [تحميل الخطوط (tar.bz2)](https://github.com/abdussalamw/mathani/releases/download/v1.0-assets/qpc_v1_by_page.tar.bz2) | تحتوي على 604 ملف خط (واحد لكل صفحة). |
| **صور الشمرلي** | [تحميل الصور (zip)](https://github.com/abdussalamw/mathani/releases/download/v1.0-assets/quran_images_shamrly.zip) | صور مصحف الشمرلي المتوافقة مع الـ 521 صفحة. |

---

## 2. الهيكل البرمجي (Code Architecture) 🏗️

### الخدمات الأساسية:
- **`QPCV1LayoutService`** (`lib/data/services/qpc_v1_layout_service.dart`):
  - يقوم بقراءة `qpc_v1_layout.db`.
  - يحول البيانات إلى كائنات `PageGlyph`.
  - يقوم بتوليد أكواد الـ PUA (مثل `0xE000 + index`) للكلمات.

- **`QPCV1FontDownloader`** (`lib/data/services/qpc_v1_font_downloader.dart`):
  - مسؤول عن تحميل الخطوط من السيرفر وفك ضغطها.
  - يقوم بتسجيل الخط برمجياً في Flutter باستخدام `FontLoader`.
  - اسم عائلة الخط لكل صفحة هو: `QPC_V1_$pageNumber`.

### شاشات العرض:
- **`MushafScreen`** (`lib/presentation/screens/mushaf/mushaf_screen.dart`):
  - يتعامل مع نوع `page_font` الخاص بالمصحف القديم.
  - يحتاج إلى `Listener` لمراقبة تغيير المصحف في الإعدادات لإعادة تحميل الصفحات (`_loadPages`).

---

## 3. ملاحظات برمجية هامة ⚠️

- **مشكلة الخط الرقمي (Digital Fallback):**
  يجب التأكد من أن `isDigital` تكون `false` في `PageLineWidget` عند اختيار المصحف القديم، وإرسال `mushafId` لربط عائلة الخطوط المخصصة.

- **توزيع الرموز (Glyph Mapping):**
  يستخدم المصحف القديم نظام PUA (Private Use Area) في الخطوط، الرموز يتم إنشاؤها في `QPCV1LayoutService` بناءً على فهرس الكلمة في السطر.

---
تم إعداد هذا الملف بواسطة **مثاني AI**.
